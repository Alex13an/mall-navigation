import { createStore } from 'vuex';
import categories from '../configs/categories';

export default createStore({
  state: {
    categories,
  },
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
